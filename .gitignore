# macOS
.DS_Store

# Node
node_modules/

# Python
.venv/
__pycache__/
*.py[cod]

# Environment files (only if ever used)
.env

# Playwright artifacts
/.playwright-mcp/
/playwright-report/
/test-results/
/blob-report/
# (Optional) downloads directory if created by automation at repo root
/downloads/

# Run-local artifacts
# Track everything under runs/ by default so run branches can fully capture state
# for rollback/debugging. If this becomes too heavy, add targeted ignores later.

# Optional per-session run notes (commit intentionally; keep README + template tracked)
/notes/agent-runs/*.md
!/notes/agent-runs/README.md
!/notes/agent-runs/_TEMPLATE.md
