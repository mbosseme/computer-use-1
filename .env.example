# Copy to .env (DO NOT COMMIT) and fill in values.
# This repo loads .env locally via python-dotenv for convenience.

# Required
AZURE_OPENAI_KEY=<your-azure-openai-key>

# Recommended: Responses API URL (GPT-5.2)
# Example:
# https://<your-resource>.openai.azure.com/openai/responses?api-version=2025-04-01-preview
AZURE_OPENAI_RESPONSES_URL=https://abi-openai-np2.openai.azure.com/openai/responses?api-version=2025-04-01-preview

# Required if not using config/models.json
AZURE_OPENAI_DEPLOYMENT=gpt-5.2-mv-2025-12-11

############################
# Microsoft Graph (Office 365)
############################

# Delegated auth (MSAL public client). Create an Entra ID app registration as a
# Public client / Mobile & desktop app and register redirect URI http://localhost.

AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_AUTHORITY_URL=https://login.microsoftonline.com/${AZURE_TENANT_ID}

# Comma-separated; runtime filters reserved scopes (offline_access, openid, profile).
# Add Mail.Read for email export/search use cases.
GRAPH_API_SCOPES=User.Read,Tasks.ReadWrite,Calendars.ReadWrite,Mail.Read

# Token cache for silent auth on subsequent runs (DO NOT COMMIT)
TOKEN_CACHE_FILE=.token_cache.json

# Optional
GRAPH_API_BASE_URL=https://graph.microsoft.com/v1.0
# Used for Outlook calendar timezone normalization (Prefer: outlook.timezone=...).
# Outlook expects a Windows timezone name (example: Eastern Standard Time).
# When printing times, prefer passing an IANA timezone via --display-timezone (example: America/New_York).
PLANNER_TIMEZONE=Eastern Standard Time

# Optional legacy / fallback (some docs use this name)
AZURE_OPENAI_URL=
