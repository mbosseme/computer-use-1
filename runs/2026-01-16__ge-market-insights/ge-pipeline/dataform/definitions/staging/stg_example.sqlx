config {
  type: "view",
  schema: "ge_sample_staging",
  description: "Example staging transformation - clean and standardize raw data",
  tags: ["staging", "example"]
}

-- Example staging query
-- Replace with actual transformation logic for your forecasting data

SELECT
  id,
  CAST(timestamp AS TIMESTAMP) as event_timestamp,
  LOWER(TRIM(product_name)) as product_name_clean,
  quantity,
  unit_price
FROM ${ref("example_raw_data")}
WHERE
  timestamp IS NOT NULL
  AND quantity > 0
