// Project-wide constants and variables for Dataform definitions
// Add forecasting-specific constants as needed

const vars = dataform.projectConfig.vars || {};

module.exports = {
  // Example: forecastHorizon: Number(vars.forecast_horizon ?? 12),
};
